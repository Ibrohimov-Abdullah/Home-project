name: firebase_learning_part2
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:

    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_auth: ^4.19.6
  firebase_core: ^2.31.1
  firebase_crashlytics: ^3.5.6
  firebase_analytics: ^10.10.6
  sign_in_button: ^3.2.0
  http: ^1.2.1
  image_picker: ^1.1.1
  email_otp: ^2.1.2
  shared_preferences: ^2.2.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/